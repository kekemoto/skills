---
status: pending
---

# <integration-unit-name>

> 1 ファイル = 1 統合単位（≒ 1 PR ≒ 1 目的 ≒ 1 検証可能単位）。
> 書くのは「何が成り立てば完了とみなせるか」。**「どう実装するか」は書かない** — 具体的な数値・ライブラリ・関数名・制御構造（setTimeout / ループ等）は受け入れ条件にしない。
> 実装順は specs ディレクトリ内のファイル名番号 `<NN>` が表す（先行する単位ほど小さい番号）。

## 要約

<この PR で何を成立させるか、2〜4 行。読者が 5 秒で全体像を掴めること。動機を添える場合も 1 行に留める>

コア受け入れ条件（詳細は下記「受け入れ条件」）:

- [ ] <最も本質的な完了条件>
- [ ] <同 2>

## スコープ

- 触る: <モジュール / ファイル / テーブル>
- 触らない: <明示的に対象外とするもの>

## 受け入れ条件

外から観測できる結果だけを書く（実装内部の関数・数値・制御フローは書かない）。
各条件に **判定方法**（自動 / 振る舞い確認）と **観測点**（どこを見れば確認できるか）を添える。

- [ ] <条件>（判定: 自動 — `npm run ...` / 振る舞い確認 — <どこで何を見るか>）
- [ ] <条件>（判定: ...）

## 失敗時の振る舞い

<どのエラーがどう扱われるか。ビジネスエラー / システムエラーの区別。エラー経路を持たない PR は「実行時挙動なし」と明記する>

## 契機 _(該当時のみ)_

<ユーザー操作・HTTP リクエスト・スケジュール・外部イベント。動線が無い PR はセクションごと省く>

## 不変条件 / 禁止 _(該当時のみ)_

- 維持: <壊してはいけない既存の振る舞い・不変条件>
- 禁止: <やってはいけないこと>

## 切り戻し _(非自明な場合のみ)_

<データ移行・整合性・デプロイ単位がからむときの戻し方。単純 revert で戻るなら省略してよい>

## 仮定 / 不確実性

- 仮定: <自律確定した非自明な前提。後で覆ると影響が出るものは「なぜ」を一言添える>
- 不確実性: <後で再確認すべき点・人間判断に上げるかもしれない論点>
